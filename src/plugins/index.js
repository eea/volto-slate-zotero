// import installZotero from './zotero-old';

// export default function install(config) {
//   return installZotero(config);
// }
import installZotero from './zotero';

export default function install(config) {
  return installZotero(config);
}
